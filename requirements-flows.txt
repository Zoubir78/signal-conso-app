# ══════════════════════════════════════════════════════════════════════════════
# requirements-flows.txt — Dépendances UNIQUEMENT pour les flows Prefect
# ══════════════════════════════════════════════════════════════════════════════

# Orchestration — doit correspondre à la version du worker/serveur Prefect
# Le worker tourne sur Prefect 3.x (task_engine.py présent, begin_task_map absent de engine.py)
prefect>=3.0,<4
prefect_gcp>=0.1,<1

# Données (utilisé dans les tasks de transformation)
pandas>=2.0,<3
numpy>=1.24,<2

# GCS (extraction du dataset)
google-cloud-storage>=2.10,<3